# Su Khing Than — Portfolio (Flutter)

Responsive portfolio built with Flutter (Web + Mobile) using Riverpod and Freezed.

## Features
- Responsive (desktop / tablet / mobile)
- Staggered animations (fade + slide)
- Smooth scroll + active nav highlighting
- Resume (Google Drive) open button
- Freezed models for Experience/Contact/Resume
- Easy to customize (data in `personal_info_repository.dart`)

## Setup

1. Install dependencies:
```bash
    flutter pub get

2. Generate Freezed code:

dart run build_runner build --delete-conflicting-outputs

3. Run the app:

flutter run -d chrome
# or
flutter run

dart run build_runner build -d


# sukhingthan_portfolio
